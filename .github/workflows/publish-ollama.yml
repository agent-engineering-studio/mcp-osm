name: Publish Ollama image (osm-mcp-ollama)
on:
  push:
    branches: [main]
    paths:
      - 'infra/ollama/**'
      - '.github/workflows/publish-ollama.yml'
  workflow_dispatch:
    inputs:
      base_model:
        description: "Base Ollama model"
        default: "llama3.1:8b"

permissions:
  contents: read
  packages: write

jobs:
  bake-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build base Ollama image
        run: docker build -t osm-mcp-ollama:bake infra/ollama

      - name: Bake model
        run: |
          docker run -d --name bake -v ollama_bake:/root/.ollama \
            --entrypoint /bin/bash osm-mcp-ollama:bake \
            -c "ollama serve & sleep 5 && \
                ollama pull ${{ inputs.base_model || 'llama3.1:8b' }} && \
                ollama create llama3.1:tools -f /Modelfile && \
                tail -f /dev/null"
          for i in $(seq 1 60); do
            docker exec bake bash -c "ollama list 2>/dev/null | grep -q 'llama3.1:tools'" && break
            sleep 10
          done
          docker exec bake bash -c "ollama list"
          docker stop bake
          docker commit bake ghcr.io/${{ github.repository_owner }}/osm-mcp-ollama:latest

      - name: Push image
        run: docker push ghcr.io/${{ github.repository_owner }}/osm-mcp-ollama:latest
